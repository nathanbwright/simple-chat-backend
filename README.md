# README

* Developed with Ruby `2.3.3` and Rails `5.1.4`

### To get up and running do the following:
Clone the repo.

Then run the following commands:
```
$ bundle install
$ rails db:migrate
$ rails s
```
